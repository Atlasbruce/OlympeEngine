#pragma once

// Minimal stub for IM Nodes API used by the editor until the real library is added
namespace imnodes_stub
{
    inline void Initialize() {}
    inline void Shutdown() {}
    inline void BeginNode(int id) {}
    inline void EndNode() {}
}
